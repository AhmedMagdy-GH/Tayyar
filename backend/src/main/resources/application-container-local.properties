# Local Compose intentionally uses HTTP. Production retains Secure cookies.
server.servlet.session.cookie.secure=false

# Keep container logs structured and on stdout even in the local stack.
logging.structured.format.console=logstash
